{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Edge",
            "type": "pwa-msedge",
            "request": "launch",
            "url": "http://localhost:5500/index.html",
            "webRoot": "${workspaceFolder}"
        }
    ]
}